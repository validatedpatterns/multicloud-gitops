version: "2.0"
secrets:
  - name: aws
    fields:
    - name: aws_key_id
      ini_file: ~/.aws/credentials
      ini_section: default
      # The below is required
      # ini_key: aws_access_key_id
