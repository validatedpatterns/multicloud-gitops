# Needed to specify the new format (missing version means old version: 1.0 by default)
version: 2.0
# NEVER COMMIT THESE VALUES TO GIT

secrets:
  - name: config-demo
    vaultPrefixes:
    fields:
    - name: secret
      value: value123
      onMissingValue: error # One of: error,generate,prompt
