version: 2.0
# NEVER COMMIT THESE VALUES TO GIT

secrets:
  - name: config-demo
    vaultPrefixes:
    - test
    fields:
    - name: secret
      value: foo
      onMissingValue: error
      base64: true
