version: "2.0"

secrets:
  - name: secret
    fields:
    - name: secret
      # Should contain 8, 6, 7, 5, 3, 0, 9 in binary
      path: '/tmp/testbinfile.bin'
      onMissingValue: error
      base64: true
