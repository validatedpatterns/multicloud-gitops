---
version: "2.0"

# This is the actual default
defaultNamespace: 'validated-patterns-secrets'

secrets:
  - name: test-secret
    type: 'user-specified'
    fields:
      - name: username
        value: user
